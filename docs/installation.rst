============
Installation
============

At the command line::

    $ easy_install django_gmail

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_gmail
    $ pip install django_gmail
